
header 
    a(href="/").header__logo
        i.fab.fa-youtube 
        span.header__logo_text  Motube Premium
    form(action="/search" , method="GET").search__form 
        input.search__form__input(type="text", placeholder="Search by title", name="keyword", required)
        button
            i.fas.fa-search
    nav 
        ul
            if loggedIn
                li 
                    a(href="/video/upload") Upload Video
                li
                    a(href="/user/edit") Edit Profile
                li
                    a(href="/user/logout")  Log Out
                li
                    a(href=`/user/${loggedInUser._id}`)
                        if loggedInUser.avatarUrl === ""
                            span 😀
                        else
                            if loggedInUser.avatarUrl
                                if loggedInUser.avatarUrl.includes("github")
                                    img.header__avatar(src=loggedInUser.avatarUrl) 
                                else 
                                    img.header__avatar(src=loggedInUser.avatarUrl)                            
            else 
                li 
                    a(href="/join").header__btn Join
                li 
                    a(href="/login") Login    