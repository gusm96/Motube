mixin video(info)
    div 
        h3 #{info.title}
        ul 
            li #{info.rating}/5
            li #{info.comments}:Comments
            li #{info.createdAt}
            li #{info.views}:Views
            li #{info.id}